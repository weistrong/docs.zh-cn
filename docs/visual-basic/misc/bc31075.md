---
description: '了解详细信息： " <elementname> " 已过时 (Visual Basic 错误) '
title: "\" <elementname> \" 已过时 (Visual Basic 错误) "
ms.date: 07/20/2015
f1_keywords:
- vbc31075
- bc31075
helpviewer_keywords:
- BC31075
ms.assetid: 614d36a1-2fba-4d03-963c-1565e88b08a6
ms.openlocfilehash: f2f1df352f292657591acde42572b049937c05e3
ms.sourcegitcommit: 10e719780594efc781b15295e499c66f316068b8
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 02/14/2021
ms.locfileid: "100459586"
---
# <a name="elementname-is-obsolete-visual-basic-error"></a><span data-ttu-id="993b5-103">" \<elementname> " 已过时 (Visual Basic 错误) </span><span class="sxs-lookup"><span data-stu-id="993b5-103">'\<elementname>' is obsolete (Visual Basic Error)</span></span>

<span data-ttu-id="993b5-104">语句试图访问编程元素，此元素已标记有 <xref:System.ObsoleteAttribute> 特性及将其视为错误的指令。</span><span class="sxs-lookup"><span data-stu-id="993b5-104">A statement attempts to access a programming element which has been marked with the <xref:System.ObsoleteAttribute> attribute and the directive to treat it as an error.</span></span>  
  
 <span data-ttu-id="993b5-105">可以通过将 <xref:System.ObsoleteAttribute> 应用于任意编程元素，将其标记为不再使用。</span><span class="sxs-lookup"><span data-stu-id="993b5-105">You can mark any programming element as being no longer in use by applying <xref:System.ObsoleteAttribute> to it.</span></span> <span data-ttu-id="993b5-106">如果执行此操作，则可以将特性的 <xref:System.ObsoleteAttribute.IsError%2A> 属性设置为 `True` 或 `False`。</span><span class="sxs-lookup"><span data-stu-id="993b5-106">If you do this, you can set the attribute's <xref:System.ObsoleteAttribute.IsError%2A> property to either `True` or `False`.</span></span> <span data-ttu-id="993b5-107">如果设置为 `True`，则编译器将尝试使用该元素的操作视为错误。</span><span class="sxs-lookup"><span data-stu-id="993b5-107">If you set it to `True`, the compiler treats an attempt to use the element as an error.</span></span> <span data-ttu-id="993b5-108">如果设置为 `False`，或将其默认为 `False`，则编译器会在有操作尝试使用该元素时发出警告。</span><span class="sxs-lookup"><span data-stu-id="993b5-108">If you set it to `False`, or let it default to `False`, the compiler issues a warning if there is an attempt to use the element.</span></span>  
  
 <span data-ttu-id="993b5-109">**错误 ID：** BC31075</span><span class="sxs-lookup"><span data-stu-id="993b5-109">**Error ID:** BC31075</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="993b5-110">更正此错误</span><span class="sxs-lookup"><span data-stu-id="993b5-110">To correct this error</span></span>  
  
- <span data-ttu-id="993b5-111">确保源代码引用的元素名称拼写正确。</span><span class="sxs-lookup"><span data-stu-id="993b5-111">Ensure that the source-code reference is spelling the element name correctly.</span></span>
